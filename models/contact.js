// const mongoose = require('mongoose')
// const { Schema, model } = mongoose

// const contactSchema = new Schema({
//       staffName: { type: String },
//       // email: { type: String },
//       // phoneNumber: { type: Number },
//       // title: { type: String },
// })

// const Contact = model('Contact', contactSchema)
// module.exports = Contact

// // Need to link this in this schema to controllers new/show edit route?
// // now the name returns an empty array, rather than nothing, in the terminal. The showpage still shows nothing
